{
  "name": "qwen-vl-bbox-tool",
  "version": "1.0.0",
  "description": "Qwen3.5-Flash BBox 标注 H5 工具",
  "scripts": {
    "dev": "wrangler pages dev public --port 8788",
    "deploy": "wrangler pages deploy public --project-name=qwen-bbox-tool"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
